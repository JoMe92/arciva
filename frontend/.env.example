# API target
# The SPA falls back to `${current-host}:8000` so LAN testing works without config.
# Set this when your API lives elsewhere (e.g. staging).
# VITE_API_BASE_URL=http://localhost:8000

# Optional HTTPS dev certs (uncomment both to enable)
# DEV_SERVER_HTTPS=true
# DEV_SERVER_HTTPS_KEY=frontend/.certs/arciva-key.pem
# DEV_SERVER_HTTPS_CERT=frontend/.certs/arciva-cert.pem
